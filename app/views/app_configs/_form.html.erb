<%= form_for([@application, @app_config]) do |f| %>
  <% if @app_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_config.errors.count, "error") %> prohibited this app_config from being saved:</h2>

      <ul>
      <% @app_config.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :config_str %><br />
    <%= f.text_area :config_str %>
  </div>
  <div class="field">
    <%= f.label :application_id %><br />
    <div class='disabled'><%= f.number_field :application_id, :value => @application.id %></div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
